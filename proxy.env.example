# Proxy Configuration Example
# Copy this file to proxy.env and modify with your proxy settings
#
# If you're having trouble downloading models from Hugging Face,
# uncomment and set the appropriate proxy settings below.
#
# Note: The project already includes httpx[socks] for SOCKS5 proxy support.

# HTTP/HTTPS proxy (replace with your proxy server)
# export https_proxy=http://127.0.0.1:7897
# export http_proxy=http://127.0.0.1:7897

# SOCKS5 proxy (replace with your proxy server)
# export all_proxy=socks5://127.0.0.1:7897

# Common proxy formats:
# HTTP proxy: http://host:port
# HTTPS proxy: https://host:port
# SOCKS5 proxy: socks5://host:port
# Proxy with auth: http://username:password@host:port